#include <iostream>
#include <typeinfo>
using namespace std;

int main() {
	
	auto inputVariable ='a'; 
	cout << "Enter some data: ";
	cin >> inputVariable;
	
	cout << "The type of the input is: " << typeid(inputVariable).name() << endl;

	return 0;
}
